procurando = input("nome que deseja: ")
nomes = ["python", "ACCER", "java", "Anaconda", "avião"]
for nome in nomes:
    if nome[0] == procurando.lower() or nome[0] == procurando.upper():
        
     print(nome)
    


    